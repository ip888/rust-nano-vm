apiVersion: v1
kind: Service
metadata:
  name: {{ include "nanovm.fullname" . }}
  labels:
    {{- include "nanovm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nanovm.selectorLabels" . | nindent 4 }}
