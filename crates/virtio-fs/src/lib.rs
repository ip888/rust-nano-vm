//! virtio-fs host ↔ guest filesystem sharing.
//!
//! Scope: **M3**. This crate is a placeholder so the workspace compiles and
//! downstream crates can depend on the name. Real implementation lands with
//! M3 — see [`docs/PLAN.md`](../../docs/PLAN.md).

#![forbid(unsafe_code)]
#![warn(missing_docs)]
